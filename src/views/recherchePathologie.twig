{% extends "header.twig" %}

{% block content %}
</br>
<h1 class="title">Recherche Pathologie</h1>
<div class="card">
    <form method="POST" action="/pathology/search">
        <div class="float-container">

            <div class="float-child">
                <div class="row">
                    <div class="col-4">
                        <?php
                        if ($requete != "") {
                            while ($a = $requete->fetch()) { ?>
                                <div class="list-group" id="list-tab" role="tablist">
                                    <input name="pathology" id="pathology" type="submit" class="list-group-item list-group-item-action" id="list-home-list" data-toggle="list" role="tab" aria-controls="home" value="<?php echo $a['desc']; ?>">
                                </div>
                        <?php }
                        }

                        ?>


                        <?php for ($i = 1; $i <= $page_total; $i++) {
                            echo '<a href="/pathology/search?page=recherchePathologie&pagination=' . $i . ' "> ' . $i . ' </a>';
                        }

                        ?>
                    </div>
                </div>
            </div>

            <div class="float-child">
                <?php

                if (isset($rows)) {
                    if (isset($pat)) {
                ?><strong><?php echo $pat; ?></strong>
                    <?php } ?>
                    <?php
                    if (count($rows) > 0) {
                        foreach ($rows as $a) {
                    ?>

                            <ul>
                                <li>
                                    <?php
                                    echo $a['descp'];
                                    ?>
                                </li>
                            </ul>
                <?php
                        }
                    }
                } else {
                    echo "aucun symptôme n'a été trouvé par la pathologie sélectionner";
                }

                ?>
            </div>
        </div>
    </form>
</div>
{% endblock %}